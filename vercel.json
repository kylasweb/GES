{
    "functions": {
        "server.ts": {
            "runtime": "@vercel/node",
            "maxDuration": 30
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/server.ts"
        }
    ]
}